{
  "name": "node-site",
  "version": "1.0.0",
  "description": "Official Node Site",
  "author": "StrixROX",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.1",
    "express": "^4.17.1",
    "node-sass": "^4.12.0",
    "nuxt": "^2.10.1",
    "sass-loader": "^7.3.1",
    "vue-particles": "^1.0.9"
  },
  "devDependencies": {
    "nodemon": "^1.19.3"
  }
}
